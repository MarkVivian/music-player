import 'package:flutter/material.dart';

class billboardAppbar extends StatelessWidget {
  const billboardAppbar({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}

class billboardBody extends StatelessWidget {
  const billboardBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}